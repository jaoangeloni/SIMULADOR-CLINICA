<%- include('../partials/head.ejs') %>
<%- include('../partials/menu.ejs') %>

<br>
<div class="container">
    <a href="/pacientes/novo" class="btn btn-primary">Novo Médico</a>
    <br>
    <div class="card">
        <div class="card-header">
            Usuários Cadastrados
        </div>
        <div class="card-body">
            <table class="table table-bordered">
                <thread>
                    <th class="col-5">Nome do Usuário</th>
                    <th class="col-5">E-mail</th>
                    <th class="col-2">Ações</th>
                </thread>
                <tbody>
                    <% usuarios.forEach(usuario => { %>
                        <tr>
                            <td><%= usuario.nome %></td>
                            <td><%= usuario.email %></td>
                            <td>
                                <a href="/pacientes/<%= paciente.id %>" class="btn btn-primary">Editar</a>
                                <a href="/pacientes/delete/<%= paciente.id %>" class="btn btn-danger">Excluir</a>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</div>


<%- include('../partials/footer.ejs') %>